/**
 * components/index.js
 * ---------------------------------------------------------------------------
 * Barrel file for shared / reusable UI components.
 *
 * Components are grouped by concern:
 *   components/
 *   ├── common/      ← Button, Input, Modal, Badge, Card, Toast
 *   ├── layout/      ← Sidebar, Header, Footer (re-exported from layouts/)
 *   └── index.js     ← This barrel file
 *
 * Example imports:
 *   import { Button, Input, Modal } from '../components';
 */

// export { default as Button } from './common/Button.jsx';
// export { default as Input } from './common/Input.jsx';
// export { default as Modal } from './common/Modal.jsx';
// export { default as Badge } from './common/Badge.jsx';
// export { default as Card } from './common/Card.jsx';
// export { default as Spinner } from './common/Spinner.jsx';
