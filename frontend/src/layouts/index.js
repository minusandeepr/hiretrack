/**
 * layouts/index.js
 * ---------------------------------------------------------------------------
 * Barrel file for layout components.
 *
 * Layout components define the page shell (sidebar, header, content area)
 * and are used as wrapper <Route> elements in AppRoutes.
 *
 *   <Route element={<DashboardLayout />}>
 *     <Route path="/dashboard" element={<DashboardPage />} />
 *   </Route>
 *
 * Layouts render an <Outlet /> where child routes appear.
 */

// export { default as DashboardLayout } from './DashboardLayout.jsx';
// export { default as AuthLayout } from './AuthLayout.jsx';
