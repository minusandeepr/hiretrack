/**
 * services/index.js
 * ---------------------------------------------------------------------------
 * Barrel file for frontend API service modules.
 *
 * Each service module wraps a group of related API calls (e.g. jobs,
 * candidates, interviews).  Re-export them here for convenient imports:
 *
 *   import { jobService, candidateService } from '../services';
 *
 * The existing api/ directory contains the Axios instance and auth API
 * wrappers created during initial setup.  New domain-specific services
 * should be added here.
 */

// export { default as jobService } from './job.service.js';
// export { default as candidateService } from './candidate.service.js';
